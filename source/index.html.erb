---
pageable: true
per_page: 10
---
<% content_for :title, "" %>
<div class="row mbm">
  <div class="mainbar prm">
    <article class="article">
      <%= partial "partials/article", locals: { article: page_articles.first } %>
    </article>
  </div>
  <div class="sidebar prm">
    <%= partial "partials/sidebar" %>
  </div>
</div>
<div class="row mbm">
  <% page_articles[1..-1].each_with_index do |article, i| %>
    <div class="column">
      <div class="box">
        <div class="header row">
          <div class="left"><%= article.title.split('.')[0] %></div>
          <% if article.title.split('.')[0].size < 20%>
            <div class="right"><small><%= article.date.strftime('%d.%m.%Y') %></small></div>
          <%end%>
        </div>
        <div class="content">
          <h2>
            <% link_to article do %>
              <%= article.title %>
            <% end %>
          </h2>
        </div>
        <div class="footer">
          <%= link_to "0 Comments and 0 Reactions", "#{default_main_url_helper}#{article.url}#disqus_thread", class: 'icon-chat' %>
        </div>
      </div>
    </div>
  <% end %>
</div>
<div class="row prm">
  <% if paginate %>
    <div class="pagination right">
      <% if prev_page %>
        <%= link_to '&laquo; Позже', prev_page, class: 'button' %>
      <% end %>
      <% if next_page %>
        <%= link_to 'Раньше &raquo;', next_page, class: 'button' %>
      <% end %>
    </div>
  <% end %>
</div>
