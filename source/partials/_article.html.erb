<h3>
  <% link_to article do %>
    <%= article_icon(article) %><%= article.title %>
  <% end %>
</h3>
<h6><%= article.date.strftime('%d.%m.%Y') %>, <%= link_to "0 Comments and 0 Reactions", "/#{article.path}#disqus_thread" %></h6>

<div class="row">
  <% if article.data.main_image %>
    <div class="eight columns">
      <%= defined?(yield_obj) ? yield_obj : article.summary %>
    </div>
    <div class="four columns">
      <img src="<%= article.data.main_image || "http://placehold.it/200x150&text=[img]" %>" alt="<%= article.title %>" />
    </div>
  <% else %>
    <div class="twelve columns">
      <%= defined?(yield_obj) ? yield_obj : article.summary %>
    </div>
  <% end %>
</div>

<div class="row">
  <div class="twelve columns">
    <ul class="inline-list">
      <% article.tags.each do |tag| %>
        <li><a href="<%= tag_path tag %>"><%= tag %></a></li>
      <% end %>
    </ul>
  </div>
</div>